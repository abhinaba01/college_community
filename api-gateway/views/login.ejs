<!DOCTYPE html>
<html>
<head>
    <title>Login | CollegeConnect</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            height: 100vh;
            font-family: 'Segoe UI', sans-serif;
        }
        /* The Glossy, Sharp Card */
        .glass-card {
            background: rgba(255, 255, 255, 0.15);
            backdrop-filter: blur(12px);
            border: 1px solid rgba(255, 255, 255, 0.2);
            box-shadow: 0 8px 32px 0 rgba(31, 38, 135, 0.37);
            border-radius: 0px; /* Sharp edges */
            color: white;
        }
        .form-control {
            background: rgba(255, 255, 255, 0.1);
            border: 1px solid rgba(255, 255, 255, 0.2);
            border-radius: 0px; /* Sharp inputs */
            color: white;
            padding: 12px;
        }
        .form-control:focus {
            background: rgba(255, 255, 255, 0.2);
            border-color: #fff;
            box-shadow: none;
            color: white;
        }
        .form-control::placeholder { color: rgba(255, 255, 255, 0.6); }
        .btn-glossy {
            background: white;
            color: #764ba2;
            border-radius: 0px;
            font-weight: bold;
            letter-spacing: 1px;
            border: none;
            transition: all 0.3s ease;
        }
        .btn-glossy:hover {
            background: #f0f0f0;
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(0,0,0,0.2);
        }
        a { color: #e2e8f0; text-decoration: none; border-bottom: 1px dashed #e2e8f0; }
        a:hover { color: white; border-bottom-style: solid; }
    </style>
</head>
<body class="d-flex align-items-center justify-content-center">
    <div class="card p-5 glass-card" style="width: 400px;">
        <h2 class="text-center mb-4 fw-light">LOGIN</h2>
        <form action="/login" method="POST">
            <div class="mb-4">
                <input type="text" name="username" class="form-control" placeholder="USERNAME" required>
            </div>
            <div class="mb-4">
                <input type="password" name="password" class="form-control" placeholder="PASSWORD" required>
            </div>
            <button type="submit" class="btn btn-glossy w-100 py-3">ENTER</button>
        </form>
        <div class="mt-4 text-center">
            <a href="/register">Create an Account</a>
        </div>
    </div>
</body>
</html>